-Create the materials class. This will handle all the textures that each mesh needs. 

This way, each time we render, we set the shader sub resource with the texures needed by the mesh that's being trendered
